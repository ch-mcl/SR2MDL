課題
---

# import/exportの位置

下記の場所から操作出来るようにしたい。

- import: メニューバー File > Import
- export: メニューバー File > Export


# exportすると法線データが変化

importして無編集でexportしたmdlファイルの法線データが異なる。

- import元: `D:\Hack\SEGAPC\SegaRally2\Tool\git_Blender\_sample\CAR\COROLLA\r_corolla.mdl`

- export後: `D:\Hack\SEGAPC\SegaRally2\Tool\git_Blender\_sample\Repack\CAR\re_export\r_corolla.mdl repack`



# 頂点の増減編集

頂点を増減させてmdlファイルを生成するとデータが壊れる。

- エラーログ: `D:\Hack\SEGAPC\SegaRally2\Tool\git_Blender\_sample\_human\error\error_log_004.txt`

- import元: `D:\Hack\SEGAPC\SegaRally2\Tool\git_Blender\_sample\CAR\COROLLA\r_corolla.mdl`

- export後: `D:\Hack\SEGAPC\SegaRally2\Tool\git_Blender\_sample\Repack\CAR\r_corolla.mdl repack`

## 操作

planeを追加してexport。
exportしたmdlのimportを行うとエラーログの18行目が発生。



# mdlファイルのマテリアル保持

SR2 MDL Materialなる専用プロパティにて保持。
保持した値はBlenderのMaterialで編集可能。
また編集した内容でexportもしたい。



# オブジェクトの一部トランフォームが未反映

回転が未反映と記憶。


# オブジェクトがX反転と90度上を向く

importしたオブジェクトが、X軸で反転と90度上を向く。
モデル改変をするうえで少し困る。



# 未解読データ
`D:\Hack\SEGAPC\SegaRally2\Tool\git_Blender\_sample\Track` のDAT(MDL内包型ファイル)について下記の解析が停まっていると記憶。

`D:\Hack\SEGAPC\SegaRally2\Tool\git_Blender\_sample\_humanWork\mdl_node_pointer_memo.md`



# テクスチャ読み取り

importしたモデルにテクスチャを適用したい。

SR2ToolsにTXR(TEX)ファイルの解析コードが既にある。

- 解析本体: `src/io_scene_srally2mdl/SR2Tools/texture_classes.py`
  RTEX / RHBG / RTXR / MTEX の4種のシグネチャに対応。
  依存は`struct`と同梱の`bmp_handler`/`unyakuza`のみなので、Blender内でもそのまま動く。

- 呼び出し口: `src/io_scene_srally2mdl/SR2Tools/texture_tool.py` の `convertTXRtoBMPlist()`
  ただしPILを使用しており、Blender同梱のPythonにPILは無い(numpyはある)。
  BMP/PNGを経由せず、解析した画素バイトを`bpy.data.images`へ直接渡す形にする必要あり。

- 各モジュールが絶対import (`from bmp_handler import BMPv5` 等) のため、
  パッケージ内から使うには相対importへの変更が必要。

## 未解決

メッシュとテクスチャの対応付けが不明。サンプル166メッシュを集計した候補は下記。

- Draw Options の `unk_0x00`
  Draw Options内で唯一値が変化するフィールド。
  0x003 / 0x103 / 0x113 / 0x803 / 0x903 / 0x1003 と、下位が0x3固定で上位ビットが立つ形。

- Model Pointers の `unk_0x18`
  通常メッシュは 10 か 1。ライト系メッシュのみ 0xE0 等のオフセット値を持ち、
  用途が重複している可能性あり(別課題の「未書き出しの追加0x20ブロック」と同じフィールド)。

レベル(DAT)の埋め込みテクスチャについては `sr2mdl.py` に
`unpack_embedded_textures_from_bytes` が既にある。

---

# 注意

記憶系は、記載者(人間側)の記憶違いである可能性あり。